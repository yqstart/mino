# 更新日志

本文件为 GitHub Release 正文提供内容（`scripts/release-notes.sh` 提取首个版本段落）。

## [1.1.1] - 2026-09-14

Agent 终端兼容版本（实证自 omp 18.1.20 二进制）。

### 新增

- kitty 键盘协议：`term::Config` 开启 `kitty_keyboard`（本地/远程/测试三处），程序订阅后非可打印键走 CSI-u（Enter=13、Tab=9、F1-F12=57345-57356、方向键 57358-57361），可打印字符永远走 legacy；释放事件编码备而不用（egui 只给 press 事件）
- 鼠标上报透传：程序订阅 1000/1002/1003 后点击/拖拽/释放直接发 xterm 鼠标序列（SGR 1006 优先，拖拽需 1002+），此前全部被本地选区吞掉，替代屏里的程序收不到点击；未订阅时本地拖选行为不变
- OSC8 超链接：同 URI 相邻 cell 合并渲染（accent2 下划线 + 悬浮小手 + 单击打开），鼠标上报订阅时点击让路给程序
- OSC52 剪贴板：程序复制经事件写系统剪贴板（失败走 toast），此前 `ClipboardStore`/`ClipboardLoad` 在 `_ => false` 被静默丢弃，"复制了但 ⌘V 粘不出来"
- Bell 转 0.6 秒视觉脉冲（左上角圆点，无常驻重绘），`ResetTitle` 接线清空程序设过的旧标题（此前同样被静默丢弃）
- 下划线变体全集：SGR 4:2 双线/4:3 波浪线/4:4 点线/4:5 虚线 + SGR 58 下划线色；粗体改前景增亮（此前直接丢弃）

### 修复

- `COLORTERM=truecolor` 注入（本地会话 + 远程 `set_env`）：omp 只认该变量判 24-bit，`TERM=xterm-256color` 只判 256 色，缺了它 agent 全程降级 256 色
- Shift+Enter 发 `ESC[13;2~`、Ctrl+Enter 发 `Ctrl+J`：omp 把提交/换行分绑裸 Enter 与 Shift+Enter/Ctrl+J，三者同发 `\r` 时输入框换不了行；裸 Enter 仍为 `\r`
- DECRQM 查 2026（同步更新）改回"不支持"：alacritty 0.26 的 2026 set/unset 是空实现却回"已重置"，程序会误判支持同步更新并全程包 BSU/ESU
- 收藏当前目录改用 shell 内核 cwd（与标题同源）：粘贴/补全/别名致输入跟踪失效后，⌘D 收藏的不再是启动目录

## [1.1.0] - 2026-09-12

项目收藏夹、终端能力应答与崩溃可诊断版本。

### 新增

- 项目收藏夹：收藏本地目录，一键打开为新终端标签（可选附带一条打开后自动执行的启动命令）；入口为标签栏文件夹按钮下拉菜单与 ⌘O 可搜索面板，设置弹窗内增删改排序，⌘D 一键收藏当前终端目录；配置与主机共用 `hosts.toml`（`[[projects]]`）

### 修复

- 补齐终端查询应答：`OSC 10/11/12`（前景/背景/光标色）、`OSC 4`（调色板）、`CSI 14 t`（文本区像素尺寸）此前**完全没有回复**（会话层把 alacritty 的 `ColorRequest`/`TextAreaSizeRequest` 当无关事件丢弃），查询终端配色的 TUI（如 omp 启动即查背景色）只能按"未知终端"回退；现由 UI 层按当前主题与 OSC 覆盖给出真实颜色
- 补齐焦点事件上报（`DECSET 1004`）：程序开启后窗口获得/失去焦点会发 `ESC [ I` / `ESC [ O`，此前从不发送，vim/tmux 等依赖焦点事件的程序状态与实际不符

### 改进

- 崩溃日志持久化到 `~/.config/mino/crash.log`（追加写入，此前写在 `/tmp` 且每次覆盖），下次启动归档为 `crash.log.1` 并提示路径——从 Dock 启动时 stderr 无人接收，闪退现场此前无法留存

## [1.0.7] - 2026-09-11

标签标题与输入法候选窗跟随版本。

### 新增

- 标签页与状态栏标题跟随本地终端当前目录：标签显示末级文件夹名（home 显示 `~`），悬浮提示与状态栏显示全路径，远程标签仍显示主机名

### 修复

- 修复中文输入法候选窗不在输入位置附近、而落在终端左下方的问题：egui-winit 0.36 定位候选窗只用 `IMEOutput.rect`（忽略 `cursor_rect`），现 `rect` 改为光标 cell 级小矩形并随光标移动同步

## [1.0.6] - 2026-09-11

中文渲染与图片粘贴版本。

### 修复

- 修复中文输入越多光标漂移越远的问题：宽字符与半角不再混排，文本分段按终端列定位绘制，恢复「终端列 = 屏幕列」对齐
- 仓库改名：`yqstart/kun` → `yqstart/mino`，更新检查、文档与测试中的仓库引用同步变更

### 新增

- 终端支持剪贴板图片粘贴：截图 `Cmd+V` 自动落盘并向命令行写入 `@路径`，可直接喂给 `omp @图` 这类吃路径的工具；Finder 复制的文件直接用原路径；远程会话经 SFTP 自动上传后再写远端路径

## [1.0.5] - 2026-09-11

中文输入、主题保持与 SFTP 传输面板优化版本。

### 新增

- 终端完整支持中文输入法：组字期间在光标处内联显示预编辑串（下划线标出当前转换节），选词后中文直接上屏写入终端，`Esc` 或切换输入法可取消未上屏的组字

### 修复

- 修复中文状态下无法向终端输入中文的问题：终端现在向后端声明输入法意图并让候选窗跟随光标，输入法才会真正激活（此前拼音组字的按键会被丢弃）

### 改进

- 主题选择持久化到配置文件，切换皮肤后重启仍保持所选主题（存主题名而非下标，旧配置缺失该字段时回落默认主题；落盘失败会回滚界面主题并提示）
- SFTP 传输进度改为标题行右缘的状态徽标：进行中（圆底上传箭头 + 总进度弧环）/ 全部完成（对勾）/ 有失败（感叹号），点击徽标展开或收起详情卡片，新传输开始时自动展开
- 无传输时不再占用面板空间，文件列表获得更多纵向空间

## [1.0.4] - 2026-09-09

窗口与 SFTP 体验补丁版本。

### 修复

- 修复 macOS 无边框标题栏双击无法在最大化与恢复之间切换的问题
- 修复设置中的主机行点击被内部文本控件遮挡，导致双击无法发起连接的问题
- 修复 SFTP“定位到终端位置”使用旧目录的问题，改为先通过 `pwd` 校正终端当前目录，并在探测超时后安全回退

### 改进

- 优化设置与连接对话框的标题区、卡片布局和关闭交互

## [1.0.3] - 2026-09-01

交互与视觉体验补丁版本。

### 修复

- 修复设置弹窗打开时终端继续消费键盘、鼠标和滚轮事件的问题
- 修复从设置面板检查更新时窗口层级错误，更新弹窗现在显示在设置窗口之上且不关闭设置
- 支持从 Finder 拖入文件、目录和应用，将经过 shell 安全转义的路径写入终端而不自动执行
- 优化 SFTP 目录切换加载状态，保留稳定布局并避免旧目录内容或传输错误造成列表闪烁

### 改进

- 重做 SFTP 传输进度、文件夹图标和确认对话框布局，提升窄面板下的可读性
- 更新应用图标与界面品牌标记为黑色哑光底、白色与磷光绿终端符号

## [1.0.2] - 2026-08-31

终端与窗口体验补丁版本。

### 修复

- 修复 macOS 新建应用窗口未自动居中的问题
- 支持根据终端中 `pwd` 的实际输出校正工作目录，修复粘贴 `cd` 等输入跟踪失效后 SFTP 仍定位到旧目录的问题
- 补充工作目录校正与窗口配置回归覆盖

## [1.0.1] - 2026-08-27

稳定性与可靠性补丁版本。

### 修复

- 修复本地终端 VT 事件、窗口标题、终端铃声和子进程退出通知在高负载或队列已满时丢失的问题
- 修复终端唤醒竞态、选区越界、触控板小幅滚轮、Shift 修饰符和输入法退格输入问题
- 修复 SSH/SFTP 连接、传输和原子重命名在关闭标签页时无法及时取消的问题
- 修复 SFTP 过期目录结果、丢失进度事件、异常远程条目名和连接中断状态处理
- 修复更新下载、安装临时文件及应用退出时的资源清理，并加强发布版本标签校验
- 补充连接生命周期、SFTP 路径安全、终端交互和更新流程的回归测试

## [1.0.0] - 2026-08-26

Mino 首个可用大版本。以轻量、快速、低干扰为目标，将本地终端、SSH 远程终端、SFTP 文件管理和多标签工作区收敛为一套完整的 macOS 工具链。

### 核心终端

- 本地终端基于 `alacritty_terminal` 与 PTY，支持 VT 仿真、ANSI 颜色、256 色、宽字符、滚动回看、闪烁光标和括号粘贴
- SSH 远程终端支持密码认证、私钥认证和私钥口令，远程会话使用 `xterm-256color`，本地终端也注入兼容的终端环境
- 键盘输入覆盖主键盘 Enter、数字小键盘、方向键、Tab、Shift+Tab、Backspace、Delete、功能键以及 Ctrl/Alt 修饰组合，按 xterm 约定编码
- 支持鼠标拖选、终端复制/粘贴、滚轮和 xterm 鼠标协议；Vim、less 等启用鼠标模式的程序可以直接使用滚轮
- 终端焦点、IME 输入、退格回显、应用键盘模式、滚动到 scrollback 后的渲染安全均有针对性处理
- 跟踪本地与远程终端可观察到的当前工作目录，SFTP 可以快捷定位到终端正在使用的目录
- 本地会话默认从用户 home 目录启动，并注入 `TERM=xterm-256color`、`CLICOLOR` 和 `LSCOLORS`，保持 Finder/Dock 启动时的终端行为一致

### SSH 与主机管理

- 支持保存主机名称、地址、端口、用户名、密码或私钥配置，TOML 文件原子写入
- 首次连接采用 TOFU 主机密钥校验，保存服务器 SHA-256 指纹；后续密钥变化会拒绝连接并给出处理提示
- SSH 与 SFTP 连接统一使用主机密钥校验、30 秒 keepalive 和无响应断开策略，降低 NAT/防火墙静默断线造成的假连接
- `~` 私钥路径自动展开，连接失败、认证失败、SFTP 中断等状态在界面中持续反馈
- `hosts.toml`、`known_hosts.toml` 及备份文件强制使用 0600 权限；配置解析失败时先备份原文件，不会静默覆盖用户数据
- 标签栏提供快速 SSH 入口，设置面板提供主机管理、双击连接和新建连接表单

### SFTP 文件管理

- 每个远程终端拥有独立的 SFTP 会话和面板，避免多标签并发时主机、列表和传输记录串台
- 支持目录浏览、上级目录、地址栏、终端目录定位、刷新和大目录虚拟化列表
- 支持上传、下载、新建目录、重命名和删除，危险操作带确认对话框
- 支持文件/目录选择、多选和右键批量操作，目录与 `..` 行支持普通双击进入/返回上级目录
- 上传/下载显示独立实时进度，完成、失败、取消和连接中断均有明确状态
- 传输失败或取消时自动清理本地/远程半成品，避免截断文件被误认为完整文件
- SFTP 面板默认收起，可拖拽调整宽度并限制在窗口合理比例内，不影响纯终端使用

### 标签页与快捷键

- 支持多个本地和远程终端标签，标签独立管理终端会话与 SFTP 状态
- 支持新建、切换、关闭标签页，标签标题使用用户设置的连接名称，不被 shell 当前路径覆盖
- 支持 `⌘T` 新建本地终端、`⌘W` 关闭当前标签、`⌘1`—`⌘9` 切换标签、`⌘N` 新建连接和 `⌘,` 打开设置
- 支持 `⌥1`—`⌥3` 快速切换主题，`⌥P` 切换性能 HUD
- 最后一个标签关闭后显示完整空状态，可从空状态直接创建本地终端或新建连接

### 界面与视觉

- 深色终端科技风格，采用应用背景、标签栏、面板、浮层和终端多层背景组织视觉层级
- 提供“深色”“深蓝”“霓虹”三套深色主题，每套拥有独立 UI token、终端前景/背景、16 色基础调色板和 xterm 256 色表
- 标签页采用主题 accent 描边、低透明度选中底色和带辉光的当前指示条，不使用白色选中线
- macOS 保留原生红绿灯按钮，无边框窗口支持圆角、透明外缘和空白标签栏拖动
- 设置面板集中管理主机、外观、更新和应用信息，控件采用紧凑圆角和低对比交互反馈
- 界面字体优先使用 macOS SF UI，终端和快捷键使用等宽字体，避免 `⌘T` 等符号发生字体回退错位
- 品牌标记使用矢量 `>_` 终端符号，避免不同系统 emoji/符号字体造成图形变化

### 性能与稳定性

- 终端渲染使用按行损坏检测、行级 Galley 缓存、显示行/scrollback 坐标映射和主题修订失效机制
- SFTP 文件列表使用可见行虚拟化，连接事件和传输进度采用事件驱动重绘
- 避免 Spinner 和常驻 60 FPS 动画造成空闲耗电，Toast、动效和性能 HUD 采用按需重绘
- 本地 PTY、远程 SSH、SFTP、窗口关闭和后台事件均有生命周期清理，关闭标签不会遗留会话或半成品传输
- 通过终端按键、滚动、焦点、选区、主题、配置、SFTP 导航、文件操作、多标签和设置交互回归测试

### 自动更新与发布

- 启动后后台检查 GitHub Releases，设置 → 关于支持手动检查更新
- 新版本弹窗显示版本和说明，支持在应用内下载 DMG、展示进度、取消/重试，并自动挂载、替换安装和重启
- 下载失败或取消会清理临时 DMG，避免损坏安装包残留
- GitHub Actions 自动构建 macOS Apple Silicon 与 Intel 版本，生成 `.app` 和 `.dmg` 安装包并上传到 Release

### 安装与已知限制

- 支持 macOS 12 及以上版本，提供 Apple Silicon（arm64）和 Intel（x64）DMG
- 安装包包含 `Mino.app` 与 Applications 快捷方式，打开 DMG 后将应用拖入“应用程序”即可安装
- 当前安装包未配置 Apple Developer 签名与公证，首次打开可能需要右键选择“打开”，或执行 `xattr -cr /Applications/Mino.app`
- 主机密码目前以明文保存在权限为 0600 的 `~/.config/mino/hosts.toml`，后续计划接入系统钥匙串

## [0.1.5] - 2026-08-25

终端主题与交互协议升级。

### 终端与主题

- 默认皮肤改为近黑石墨底 + 终端绿 accent + 琥珀辅助色，终端调色板同步更换，去除终端右上角柔光装饰
- 鼠标滚轮按 xterm 鼠标协议上报（SGR / UTF-8 / X10 兼容），Vim、less 等启用 DECSET 鼠标模式的应用可正常滚动
- Esc 键仅关闭前台弹窗，无弹窗时转发给终端（Vim 退出插入模式可用）

### 界面

- 性能 HUD 移入底部状态栏右侧并默认开启（⌥P 仍可切换）
- SFTP 错误信息截断显示，避免超长错误撑破状态栏

## [0.1.4] - 2026-08-25

修复 SFTP 面板的三处文件操作体验问题。

### SFTP

- 上传与传输进度区域移出文件列表底部的不可见区域：选择文件后立即可见实时进度，不再"点了没反应、等一会才发现传好了"
- 目录单击直接进入，`..` 单击返回上级（按住 ⌘/Ctrl/Shift 单击目录仍只选中，批量操作不受影响）——此前需要双击或"选中后再点一次"，首次点击没有任何视觉变化
- 定位到终端目录（右键菜单项 / ⌘⇧L）增加明确反馈：跳转成功提示"定位到终端目录 …"，目标与当前目录相同时提示"已在终端目录 …"，不再静默无反应

## [0.1.3] - 2026-08-25

本版本聚焦连接稳定性、更新安全性、终端兼容性和配置数据保护。

### 稳定性与 SFTP

- 为 SSH 连接、认证和远程会话增加超时与断线处理，避免界面永久停留在连接中
- 支持取消 SFTP 传输，关闭标签页或连接时立即中止操作并清理半成品
- 修复终端过期选区越界崩溃，以及终端写入队列满时丢失数据的问题
- 避免 SFTP 下载完成后重复刷新远程目录

### 安全性与数据保护

- 更新安装前校验应用签名，下载包校验 Content-Length 与大小边界
- 收紧更新临时目录权限，修复 hosts.toml、known_hosts.toml 及备份文件的权限窗口
- 强化 hosts.toml 原子写入、失败清理与 fsync，避免配置损坏或被截断

### 终端与交互

- 补全 Ctrl 标点及 Alt+Ctrl 组合键编码，修复 Menlo 符号字体 fallback 顺序
- 修复设置与连接弹窗的 Esc 关闭行为，以及 Cmd+N 打开弹窗时连接表单重置
- 修复 `cd ~` 与远程复合命令的工作目录解析
- 修复平滑动画停在中间状态时未继续重绘的问题

## [0.1.2] - 2026-08-21

本版本聚焦终端交互、SFTP 导航和界面细节修复。

### 终端与交互

- 移除应用内命令提示浮层、候选计算和幽灵文本，Tab、方向键与 Esc 交还 shell 直接处理
- 保留本地与远程终端的 `cd` 工作目录跟踪，SFTP 可从终端当前目录快捷定位
- 标签页固定显示用户设置的连接名称，避免被 shell 路径标题覆盖
- 新建连接对话框每次打开都重置表单，主机管理卡片的列布局保持稳定

### SFTP

- 规范化终端目录与 SFTP 路径，区分 SFTP home，支持 `~`、相对路径和 `.`/`..`
- 忽略过期目录请求的异步结果和错误，避免快速切换目录时内容串回
- 改进目录双击进入、加载区域稳定性、错误清理和多选右键交互

### 界面与维护

- 默认主题调整为紫夜配色，macOS 窗口保留原生交通灯按钮
- 补充工作目录跟踪测试、命令提示移除回归测试及全量代码审查清单
- 发布前通过工作区编译、Clippy、格式检查和 `mino-app` 全部 66 个测试

## [0.1.1] - 2026-08-21

针对首个版本反馈的一轮安全性、稳定性和发布体验修复。

### 安全性

- known_hosts 文件读取、解析或保存失败时改为拒绝连接，避免异常状态绕过主机密钥校验
- 过滤括号粘贴内容中的转义字符，避免输入内容注入终端控制序列
- 终端关闭前确认子进程仍为原进程，避免 PID 复用时误发信号
- 更新 SSH 测试服务的停止逻辑，避免误杀其他进程
- 补充 TOFU 主机密钥校验、文件权限和明文密码限制的安全说明

### 稳定性

- 修复多标签页 SFTP 结果和传输记录的身份串台，并清理失败连接及半成品传输
- 限制终端和 SFTP 事件积压，修复终端组合字符、隐藏列、背景色和主题切换后的缓存问题
- 修正特殊键修饰符编码，提升终端兼容性
- 修复主机配置保存失败时的状态反馈和数据保护
- 修复更新下载取消、重试、超时、安装状态反馈及后台进度重绘

### 测试与发布

- 隔离 UI 测试配置，避免测试影响用户主机配置
- 改善 SSH 集成测试在无测试服务环境下的行为，并修复主题截图测试
- 发布工作流校验 tag、Cargo workspace 版本和 CHANGELOG 版本一致

## [0.1.0] - 2026-08-20

Mino 首个可用版本，聚焦轻量、简洁、快速的本地与远程终端体验。

### 核心能力

- 本地终端：基于 `alacritty_terminal`，支持 VT 仿真、256 色、宽字符、滚动和括号粘贴
- SSH 远程终端：支持密码与私钥认证，远程会话使用 `xterm-256color`
- SFTP 文件管理：目录导航、上传、下载、新建目录、重命名、删除和传输进度
- 主机管理：TOML 配置持久化，支持快速 SSH 连接
- 多标签页：本地与远程终端独立管理，支持新建、切换和关闭

### 交互体验

- 深色科技感界面，统一的面板、标签、按钮和输入框视觉层级
- 终端支持鼠标拖选、`⌘C` / `Ctrl+Shift+C` 复制和复制反馈
- Warp 风格命令提示：幽灵文本、候选浮层、Shell 内置命令、Tab/方向键/Esc 操作
- 三套深色主题，支持设置面板与 `⌥1`—`⌥3` 快速切换
- SFTP 面板默认收起，按需展开，不干扰纯终端使用

### 稳定性与安全

- SSH 主机密钥采用 TOFU 校验，检测服务器密钥变化
- SSH keepalive、私钥路径展开和失败连接状态提示
- 上传/下载失败自动清理半成品文件
- 终端按键、焦点恢复、滚动、补全定位和 macOS 无边框窗口均有回归测试
- 支持 macOS Apple Silicon 与 Intel，提供 `.app` 和 `.dmg` 安装包

### 已知范围

- 当前仅支持 macOS 12 及以上版本
- 安装包暂未进行 Apple 公证，首次打开可能需要在系统提示中确认
