# 更新日志

本文件为 GitHub Release 正文提供内容（`scripts/release-notes.sh` 提取首个版本段落）。

## [1.0.1] - 2026-09-15

修复自管 GPU 渲染滚动时的内容覆盖：`gpu_row_meshes` 行顶点缓存曾按显示行号索引，而行顶点是行内相对坐标、内容只与网格行相关——滚动后旧显示行的顶点错配到新网格行，表现为 omp/claude 等高频重绘应用输出后滚轮查看历史时文字/横线压在背景上。构建、上传判脏、缓存裁剪统一按网格行号索引，uniform 行原点仍按显示行给出（滚动只换 uniform、零重传）。

## [1.0.0] - 2026-09-15

Mino 全量首版：轻量、快速、低干扰的 macOS 终端工具链，把本地终端、SSH 远程终端、SFTP 文件管理和多标签工作区收敛为一套完整应用。本版本合并此前 1.0.0~1.1.2 全部能力，一次性完整发布。

### 核心终端

- 本地终端基于 `alacritty_terminal` 与 PTY，支持 VT 仿真、ANSI 颜色、256 色、24-bit 真彩色、宽字符、滚动回看、闪烁光标和括号粘贴
- SSH 远程终端支持密码认证、私钥认证和私钥口令；本地注入 `TERM=xterm-256color`、`CLICOLOR=1`、`LSCOLORS`、`COLORTERM=truecolor`，远程经 `set_env` 同步注入（sshd 未放行时静默忽略），与 Terminal.app/iTerm2 行为一致
- 键盘按 xterm 约定编码：主键盘 Enter 永远发 `\r`（应用键盘模式只影响数字小键盘）；Shift+Enter 发 `ESC[13;2~`、Ctrl+Enter 发 `Ctrl+J`（omp 提交/换行分绑）；方向键按 APP_CURSOR 切 SS3；Backspace=`\x7f`、Delete=`\x1b[3~`、Shift+Tab=`\x1b[Z`；F1-F4 带修饰键时末尾字母按 P/Q/R/S 递增；Ctrl/Alt 标点组合补全
- kitty 键盘协议：`term::Config` 开启 `kitty_keyboard`（本地/远程/测试三处），程序订阅后非可打印键走 CSI-u，可打印字符永远走 legacy
- 鼠标上报透传：程序订阅 1000/1002/1003 后点击/拖拽/释放直接发 xterm 序列（SGR 1006 优先，拖拽需 1002+）；未订阅时走本地拖选；滚轮按 xterm 鼠标协议上报，Vim/less 可直接滚动
- 终端能力应答齐全：`OSC 10/11/12`（前景/背景/光标色）、`OSC 4`（调色板）、`CSI 14 t`（文本区像素尺寸）按当前主题与 OSC 覆盖回真实颜色；`DECRQM 2026` 回不支持（alacritty 空实现回"已重置"会误导程序包 BSU/ESU）；焦点事件 `DECSET 1004` 按窗口焦点发 `ESC [ I` / `ESC [ O`
- OSC8 超链接：同 URI 相邻 cell 合并渲染（accent2 下划线 + 悬浮小手 + 单击打开），鼠标上报订阅时让路给程序；OSC52 剪贴板：程序复制经事件写系统剪贴板（失败走 toast）；Bell 转 0.6s 视觉脉冲；`ResetTitle` 清空程序设过的旧标题
- 下划线变体全集：SGR 4:2 双线/4:3 波浪/4:4 点线/4:5 虚线 + SGR 58 下划线色；粗体改前景增亮（egui 无合成粗体）
- 终端写回队列 64KB 有界：后台标签长期不消费时丢最旧的应答（程序按"未知终端"回退），内存有界
- 本地会话默认从用户 home 目录启动；`hosts.toml` 解析失败先备份再按空配置启动并提示，不静默覆盖

### 中文输入与中文渲染

- 终端完整支持中文输入法：聚焦时声明 `IMEPurpose::Terminal` 意图并跟随光标 cell 级小矩形同步候选窗位置；`Preedit` 只做内联下划线渲染（不进 PTY）、`Commit` 才上屏写入、`DeleteSurrounding` 转退格/删除序列；弹窗打开时不声明 IME
- 微信输入法退格伴随空格类 Text 事件已过滤（跨帧标记，仅丢空白字符）；`TERM=dumb` 根因已根除（GUI 启动强制注入 TERM）
- CJK 宽字符逐 cell 成段、按终端双列起点定位：同段多字混排时字体 advance 与终端列宽差 3.1px/字累积漂移的问题消除；宽字符 Galley 按（字符，样式）跨行跨帧缓存；零宽组合符整体 shaping 不入缓存
- 字体链：SF Mono（主）+ Menlo（符号 fallback，补 `➜`/`❯` 等提示符缺字）+ 中文字体 + egui 默认；禁止加载 Apple Color Emoji（ab_glyph 无法解析会 panic）
- 中文字体（55MB+ `.ttc`）后台线程读取、就绪后增量并入；渲染侧按字体定义指纹在帧开头比对命中才清行缓存，避免并入当帧用旧字体重建导致乱码常驻

### 渲染性能

- 行级增量渲染：damage 行 + 列区间只重建受损列（光标移动只损 1-2 列）；行缓存按网格行号索引（`hash` 含 fg/bg/样式/字符/link），滚动后直接命中；一行 N 段合成单个 `Mesh` 批量提交；装饰网格线/IME 预编辑串加缓存；目录读取 300ms TTL；一帧输入合并一次 `write`；resize 通知 50ms 节流；远程按包唤醒合并为每帧一次
- 终端自管 GPU 渲染：每行顶点常驻显存 + 逐行 uniform，内容不变不重传、滚动只改行原点、空闲帧零上传；管线逐项对齐 egui（颜色/混合/抖动/gamma/预乘）；图集换代看门狗按「尺寸变化或填充率骤降」双条件检测并在下一帧开头复核；无 wgpu 环境自动回退 egui Shape 路径
- 持续重绘治理：`Spinner` 全换静态加载指示；toast 动画期后只安排到期帧；Bell 脉冲只安排到期一帧；性能 HUD（⌥P，默认开启）显示帧耗时/FPS/构建分段/Shape 数/重建行/上传字节与「首帧」「终端就绪」耗时
- 本地会话后台创建：PTY fork + oh-my-zsh 初始化不再堵首帧与 ⌘T，就绪后自动挂标签，等待期状态栏显示"终端启动中…"
- 远程与 SFTP 共用进程级 tokio runtime（上限 8 worker），多标签线程数不再线性增长；发布构建 thin LTO + 只剥离 debuginfo（保留符号名供崩溃日志定位）

### SSH 与主机管理

- 主机配置（名称/地址/端口/用户名/密码或私钥）存 `~/.config/mino/hosts.toml`，原子写 + 0600 权限；`~` 私钥路径自动展开；30s keepalive + 3 次无响应断开
- 首次连接 TOFU 主机密钥校验：记录服务器 SHA-256 指纹到 `known_hosts.toml`（0600），后续不一致拒绝并给出删除对应条目的修复指引；终端与 SFTP 双连接共用同一校验入口，进程级锁串行化读-改-写
- 标签栏 `>_` 快速 SSH 菜单：单击即连；设置弹窗主机管理：单击选中、双击连接；新建连接表单默认值 root/22；连接失败/认证失败/SFTP 中断在状态栏持久显示

### SFTP 文件管理

- 每个远程 tab 独享 SFTP 会话与面板，多标签不串台；独立 SSH 连接跑在共享 runtime 上，会话存活到 `Shutdown` 命令
- 目录浏览/上级 `..` 置顶行/地址条/刷新/终端目录定位；大目录 `show_rows` 虚拟化；事件驱动重绘（进度/列表刷新即时可见）
- 上传/下载/新建目录/重命名/删除（含确认框、右键批量）；传输进度平时只显示标题行右缘状态徽标（进行中/完成/失败），点击展开详情，新传输自动展开；失败或取消自动清理本地/远程半成品
- 文件时间用精确 UTC 换算（含闰日回归）；面板默认收起、右上角悬浮 "SFTP" 按钮切换，宽度默认窗口 40%、上限 50%，顶层面板注册避免错位覆盖终端

### 标签页与项目收藏

- 多标签（Warp 风格）：本地/远程独立会话与 SFTP 状态，点击切换/×关闭/＋新建；本地标签标题跟随当前目录末级名（home 显示 `~`），悬浮与状态栏显示全路径（shell 内核 cwd 优先，跟踪值兜底）；远程标签显示主机名
- 项目收藏夹：收藏本地目录 + 可选启动命令，入口为标签栏菜单与 ⌘O 搜索面板，⌘D 收藏当前终端真实目录；配置与主机共用 `hosts.toml`（`[[projects]]`）
- 快捷键：⌘T 新建本地、⌘W 关闭、⌘1-9 切换、⌘N 新建连接、⌘, 设置、⌘O 收藏搜索、⌘D 收藏当前目录、⌥1-3 主题、⌥P 性能 HUD；最后一个标签关闭后显示空状态

### 界面、窗口与主题

- Tabby 风格深色界面：分层背景/三阶文字/accent 蓝 + 青蓝辅助/语义色；hover 白低透明叠加，选中才用 accent 底；圆角 6px/5px；按钮紧凑；终端光标白色
- 三套深色主题（深色/深蓝/霓虹），各含 UI token + 终端调色板（16 色 + xterm 256 色表）；切换即落盘（存主题名），启动按名恢复；主题测试串行
- macOS 保留原生交通灯 + 透明标题栏；无边框窗口整体圆角（layer 12px + 透明外缘）；标签栏空白处拖拽移窗、双击 zoom；设置弹窗卡片化（主机/外观/关于），主机行 accent 头像 + 超长截断 + hover 删除
- 品牌 logo：紫金渐变圆底 + 白色粗体 "K"；动效无第三方依赖（指数平滑/扫光/渐变/辉光）；hover 小手，输入框 I-beam
- 终端剪贴板图片粘贴：截图 ⌘V 落盘后写 `@路径`（远程经 SFTP 自动上传）；支持 Finder 拖入文件/目录/应用（shell 安全转义，只写不执行）

### 稳定性与可诊断

- 锁序铁律「先终端锁、后 Context」：滚动/重绘/时间读取一律记意图在锁外执行，SSH + Agent 双窗口整窗无响应（AB-BA 死锁）已修复并有回归覆盖
- 本地会话关闭兜底：SIGHUP 后守护线程 300ms 补 SIGKILL，避免 zsh 不响应 SIGHUP 卡死 UI；`Session::Drop` 发 Shutdown 优雅关闭
- 崩溃日志追加写 `~/.config/mino/crash.log`（写失败回落 `/tmp/mino-panic.log`），下次启动归档为 `crash.log.1` 并提示路径
- 应用内更新：`releases.atom` 检查，`mino-{版本}-macos-{arm64|x64}.dmg` 直链下载（进度/取消/重试，失败清半成品），后台脚本挂载替换重启；macOS 打包补文件夹用途说明

### 安装与已知限制

- 支持 macOS 12+，提供 Apple Silicon（arm64）与 Intel（x64）DMG；DMG 内含 `Mino.app` 与 Applications 快捷方式，拖入即安装
- 安装包未做 Apple 签名与公证：首次打开需右键"打开"，或执行 `xattr -cr /Applications/Mino.app`
- 主机密码明文保存在 0600 的 `hosts.toml`，后续计划接入系统钥匙串
- 自管 GPU 路径 kittest 覆盖不到，需实机验证（HUD 空闲帧 shapes 个位数、上传接近 0）
